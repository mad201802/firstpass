{
    "scripts": {
        "build": "cd backend && mvn clean package && cd ../frontend && yarn build",
        "start": "cd frontend && yarn run dev",
        "dist": "npm run build && cd frontend && yarn dist"
    }
}